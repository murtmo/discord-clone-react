import styles from "./ChatStart.module.scss";

const ChatStart = () => {
  return (
    <div className={styles.chatStart}>
      <p className={styles.chatStartTitle}>トークルームを選んでください🐢</p>
    </div>
  );
};

export default ChatStart;
